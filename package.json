{
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run --headless --browser chrome"
  },
  "devDependencies": {
    "@testing-library/cypress": "^10.0.1",
    "cypress": "^13.7.0",
    "typescript": "^5.4.2"
  }
}
